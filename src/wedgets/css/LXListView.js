import {
    Platform,
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    loading:{
        height: 50,
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
    },
    otherFooter:{ 
        height: 50, 
        justifyContent: 'center', 
        alignItems: 'center' 
    }
    
});

export default styles;